package es.unican.gasolineras.model;

/**
 * Enum representing the order types for sorting.
 * It specifies whether the sorting should be in ascending or descending order.
 */
public enum Orden {
    ASCENDENTE,
    DESCENDENTE
}
